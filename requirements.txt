# CLI dependencies — shared src/ modules used without the FastAPI server.
# The API uses api/requirements.txt; this file covers standalone pipeline.py runs.

# Azure SDKs
azure-storage-blob==12.19.1
azure-ai-vision-imageanalysis==1.0.0
openai>=1.50.0                   # Azure OpenAI uses the openai package

# Utilities
python-dotenv==1.0.1
requests==2.31.0
